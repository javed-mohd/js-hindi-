// # primitive datay type (string, number, boolean, null, undefined, symbol, BigInt)

// non-primitive (Array, object, function)


const ID = Symbol('100');
const anotherID = Symbol('100') // work to make uneaque

console.log(ID === anotherID); // false


